class CenteredCameraComponent extends Component{
    start(){
        // this.transform.x = 0;
        // this.transform.y = canvas.height/2;
        // this.transform.sx = 3;
        // this.transform.sy = 3;

    }
    update(){

        // let plane = GameObject.getObjectByName("plane").getComponent("planeComponent"); 
        // this.transform.x = plane.transform.x;
        // this.transform.y = plane.transform.y;
    }
}

window.CenteredCameraComponent=CenteredCameraComponent;
